apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movies_app_helm_repo.fullname" . }}-test-connection"
  labels:
    {{- include "movies_app_helm_repo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movies_app_helm_repo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
