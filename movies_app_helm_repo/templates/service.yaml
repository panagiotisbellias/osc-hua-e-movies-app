apiVersion: v1
kind: Service
metadata:
  name: {{ include "movies_app_helm_repo.fullname" . }}
  labels:
    {{- include "movies_app_helm_repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movies_app_helm_repo.selectorLabels" . | nindent 4 }}
